# Content follow-ups

Operator notes for copy and media that is planned but not yet in the repo.

## Model photos — galvanised stock (pending from Rob)

Current product and gallery images show **pre-galvanised / older DeSite stock**. Australian models are now **galvanised**; site imagery will look outdated until refreshed.

**Ask:** Rob to supply **5 new photos per model** (hero + gallery angles on real AU stock).

**When files arrive:**

1. Drop images under `public/images/` (consider `public/images/galvanised/` per model).
2. Update hero/gallery paths in product content (`src/content/slg-*.js`, `static-grizzly.js`, etc.).
3. Update home equipment cards and Instagram story rings in `src/content/home.js`.
4. Refresh `src/data/imageCatalog.js`, `src/content/photos.js`, and OG images in `src/data/seo.js` where a model hero is referenced.

**Models to cover (minimum):** SLG-108VFRB, SLG-78VF, SLG-78VF Flow, SLG-68V, SLG-78 Static Grizzly, SLG-108 Static Grizzly, mini screeners (56/48), dump trailers, skid-steer attachments, telehandler bins, grizzly bar.

Until then, existing photos remain live — no placeholder swap required.
